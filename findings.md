# Findings
- `styleprd.md` defines a custom map screen with absolute positioned overlays, a floating bottom tab bar, a top nav bar, and a sidebar.
- `Campus-map` currently uses expo-router `Tabs` with `index`, `profile`, `settings`.
